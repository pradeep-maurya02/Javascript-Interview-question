// for a given arrya of number , print the square of each value using th forEach loop.
let marks=[87,93,64,99,86];
let topper = marks.filter((val) => {
  return val > 90;
}); 
console.log(topper);